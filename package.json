{
  "name": "api-tests-postman-newman",
  "version": "1.0.0",
  "scripts": {
    "test": "node load-schema.js && newman run collections/viacep.postman_collection.json -e environments/viacep.postman_environment.json -e runtime.environment.json -r htmlextra --reporter-htmlextra-export reports/report.html"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "newman": "^6.1.1"
  },
  "devDependencies": {
    "newman": "^6.0.0",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
